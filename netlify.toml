# netlify.toml
[build]
  command = "npm ci && npm run build"
  publish = "dist/qa-portfolio-angular/browser"
  framework = "#static"   # Force static: prevents Netlify from auto-attaching the Angular runtime

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
