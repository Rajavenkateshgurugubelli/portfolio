# Angular 18 Portfolio — Netlify Ready (No Plugins)

## Local
npm install
npm run dev

## Build
npm run build  # outputs to dist/portfolio/browser

## Netlify
- Build command: npm install && npm run build
- Publish directory: dist/portfolio/browser
- SPA redirects provided via netlify.toml and src/_redirects
